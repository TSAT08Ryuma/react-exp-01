// rafce

import React from 'react'

const Button = (props) => {
  return (
    <div className="aa">
        {/* この下に目印(props)を表示します */}
    <div>
      <div>{props.text}</div>
    </div>

        {/*  */}
    </div>
  )
}

export default Button